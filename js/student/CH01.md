# Computer System Overview

## Operating System
1. Exploits the **hardware resources** of one or more processors
2. Provides a set of **services** to system users
3. Manages **secondary memory** and **I/O devices**
4. Provides support for **networking** and **communications**
## Basic Elements of Computer System

### 1. Processor
1. Controls the **operation** of the computer
2. Performs the **data processing** functions
3. Referred to as the **Central Processing Unit (CPU)**
### 2. Main Memory
1. Stores **data** and **programs**
2. Typically **volatile** 
	- Contents of the memory is lost when the computer is shut down
3. Referred to as **real memory** or **primary memory**
### 3. I/O Modules
- Move data between the computer and its **external environment**
	1. **Secondary memory devices** (e.g. disks)
	2. **Communications equipment**
	3. **Terminals**
### 4. System Bus
- Provides for **communication** among processors, main memory, and I/O modules

![[Fig1-01.png]]

## Processor Registers
- **Faster and smaller** than main memory, has two types:
### 1. User Visible Registers
1. **Minimize main memory access** by optimizing register use
2. May be referenced by **machine language**
3. Available to **all programs** (application and system programs)
#### Data Registers
- Can be changed by user
#### Address Registers
- Memory addresses of data and instructions
	- **Index registers**
	- **Segment pointers** -> (used in memory addressing)
	- **Stack Registers** -> (User / Supervisor stacks) 
### 2. Control and Status Registers
- May or may not be visible
1. **Program Counter (PC)** -> address of next instruction
2. **Instruction Register (IR)** -> most recent fetched instruction
3. **Condition Codes** -> results of operations
4. **MAR/MBR** -> memory reference registers
5. **Program Status Word (PSW)** condition codes, interrupts, mode
#### Program Status Word (PSW) Register
- All processor designs include a **PSW register**
- PSW contain **condition codes** plus other **status information**
- A good example of a PSW is that on **Pentium processors**

![[Fig3-12.png]]

## Microprocessor
- Invention that brought about **desktop and handheld computing**
- Contains a processor on a **single chip**
- **Fastest** general Purpose processors
- **Multiprocessors**
	- Each chip (socket) contains **multiple processors (cores)**
## Graphical Processing Units (GPU's)
- Provide efficient computation on arrays of data using a **Single Instruction Multiple Data (SIMD)** techniques pioneered in supercomputers
- No longer used just for rendering advanced graphics
- Also used for **general numerical processing**
	- Physics simulations for games
	- Computations on large spreadsheets

## Digital Signal Processors (DSPs)

- Deal with streaming signals such as **audio or video** 
- Used to be embedded in I/O devices like **modems** 
	- Are now becoming **first-class computational devices**, especially in handhelds 
- **Encoding/decoding** speech and video (codecs) 
- Provide support for **encryption and security**

## Basic Instruction Cycle

- A program consists of a set of **instructions** stored in memory
- Two steps:
1. **Fetch**:
	- The processor fetches the instruction from memory
		- Typically the **program counter (PC)** hold the address of the next instruction to be fetched
		- PC is incremented after each fetch
2. **Execute**:
		- Fetched instruction is loaded into **Instruction Register (IR)**
		- Processor **decode** (interprets) the instruction and performs the required action

![[Fig1-02.png]]

### Types of Instructions
1. **Processor <-> memory**: Transfer data between processor and memory
2. **Processor <-> I/O**: data transferred to or from a peripheral device
3. **Data Processing**: arithmetic or logic operation on data
4. **Control**: alter sequence of execution (e.g. jump, branch etc.)

![[Fig1-03.png]]

![[Fig1-04.png]]

## Interrupts

- Mechanism by which other modules may **interrupt** the normal sequencing of the processor
- Provided to **improve processor utilization**
	- Most I/O devices are **slower** than the processor
	- Processor must **pause to wait** for device
	- Wasteful use of the processor

### Classes of Interrupts

![[Tab1-01.png]]

### Flow of Control

- A PC that operates at $1 GHz$, allow $≈ 10^9$ instructions per second ($1$ instruction costs $1 ns$ )
- A typical hard disk has a rotational speed of $7200$ revolutions per minute for a half-track rotation, result in reading time of $4 ms$, which is **$4$ million times slower** than the processor

#### Short I/O Wait

![[Fig1-08.png]]
#### Long I/O Wait

![[Fig1-09.png]]
### Interrupt Handler (ISR - Interrupt Service Routine)
- Whenever there is an interrupt, control is **transferred** to this program.
- It determines the **nature** of the interrupt and performs the **necessary actions** to handle it.

![[Fig1-06.png]]

### Instruction Cycle with Interrupts
- At the end of each cycle, processor **checks for interrupts**
	- **If no interrupts** -> fetch the next instruction for the current program 
	- **If an interrupt is pending** -> suspend execution of the current program, and execute the **interrupt handler (ISR)** 

![[Fig1-07.png]]

### Simple Interrupt Processing

![[Fig1-10.png]]

### Changes in Memory and Registers for an Interrupt

![[Fig1-11.png]]

### Multiple Interrupts
- Suppose **multiple interrupts** received
#### Approach 1: Disable Interrupts During Interrupt Processing 
- Interrupts remain **pending** until the processor enables interrupts
- After interrupt handler routine completes, the processor **checks** for additional interrupts
#### Approach 2: Use a Priority Scheme

| Approach      | Disabled Interrupts                                                        | Priority Scheme                                              |
| ------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Advantages    | Simple                                                                     | Lower-priority interrupt handler can be interrupted (nested) |
| Disadvantages | Interrupts are handled in strict sequential order; no account for priority | Complex                                                      |

### Transfer of Control with Multiple Interrupts
![[Fig1-12.png]]

### Example Time Sequence of Multiple Interrupts
- Three I/O devices:
	- Printer (priority 2)
	- Disk (priority 4)
	- Communications line (priority 5)

![[Fig1-13.png]]

## Storage Device Hierarchy

- **Design constraints** on a computer's memory:
	- How much?
	- How fast?
	- How expensive?
- Memory must be able to keep up with the processor
- If the capacity is there, applications will likely be developed to use it
- Cost of memory must be reasonable in relationship to the other components

### Memory Relationships
- Faster access time = greater cost per bit
- Greater capacity = smaller cost per bit
- Greater capacity = slower access speed

### The Memory Hierarchy

![[Fig1-14.png]]

![[Fig1-14C.jpg]]

- Going down the hierarchy:
	1. Decreasing cost per bit
	2. Increasing capacity
	3. Increasing access time
	4. Decreasing volatility
	5. Decreasing frequency of access to the memory by the processor (Key success trade-off)
- **Principle of Locality**:
	- Memory references by the processor tend to **cluster**
	- Data is organized so that the percentage of accesses to each successively lower level is substantially less than that of the level above
- **Secondary Memory** (Auxiliary memory):
	- External, nonvolatile, used to store program and data files
- Movement between levels of storage hierarchy can be explicit or implicit

![[Fig1-14B.jpg]]

## Performance of a Simple Two-Level Memory

![[Fig1-15.png]]

- **Two levels of memory**:
	- **Level 1** contains 1,000 bytes and has an access time of $T_1 = 0.1\ \mu s$
	- **Level 2** contains 100,000 bytes and has an access time of $T_2 = 1\ \mu s$

## Cache Memory
- Important principle, performed at many levels in a computer (in hardware, operating system, software)
- **Invisible** to the OS and interacts with other memory management hardware
- Exploit the **principle of locality** with a small, fast memory
- Information in use is copied from slower to faster storage temporarily
	- Faster storage (cache) is checked first
	- If it is there (**hit**), information is used directly from the cache (fast)
	- If not (**miss**), data is copied to cache and used there
- Cache is smaller than the storage being cached
- Processor must access memory at least once per instruction cycle; processor execution is limited by memory cycle time

### Cache Design
- Cache management is an important design problem involving:
	- **Cache size**: Small caches have a significant impact on performance
	- **Block size**: The unit of data exchanged between cache and main memory
	- **Mapping function**: Determines which cache location the block will occupy
		- Constraints: When one block is read in, another may have to be replaced
		- The more flexible the mapping function, the more complex is the circuitry required to search the cache
	- **Replacement algorithm**: 
		- E.g., **Least Recently Used (LRU)** algorithm replaces a block that has been in the cache the longest with no references
		- Hardware mechanisms are needed to identify the LRU block
	- **Write policy**: Dictates when the memory write operation takes place
		- Can occur every time the block is updated
		- Can occur when the block is replaced (Minimizes write operations, but leaves main memory in an obsolete state)
	- **Number of cache levels**

![[Fig1-16.png]]
![[Fig1-17.png]]
![[Fig1-18.png]]

## I/O Techniques

- When the processor encounters an I/O instruction, it executes that instruction by **issuing a command** to the appropriate **I/O module**
- Three techniques are possible for I/O operations:

### 1. Programmed I/O
- Processor sends a **request** to the I/O module
- The I/O module performs the requested action and sets the appropriate bits in the I/O status register, but **does not interrupt** the processor when done
- The processor periodically **checks the status** of the I/O module (**polling**) until it determines the instruction is complete
- The processor is responsible for the **entire data transfer**:
	- **For Output**: Extracting data from main memory and sending it to the I/O module
	- **For Input**: Extracting data from the I/O module and storing it in main memory
- **Disadvantage**: Keeps the processor busy needlessly (**polling wastes CPU cycles**, severely degrading the overall system performance)

### 2. Interrupt-Driven I/O
- Processor issues an I/O command to a module and then goes on to do **some other useful work**
- The I/O module will then **interrupt** the processor to request service when it is ready to exchange data
- The processor executes the data transfer and then **resumes** its former processing
- **Advantage**: 
	- Free to do other work 
	- **No needless waiting** 
	- More efficient than Programmed I/O
- **Disadvantage**: 
	- Consumes a lot of processor time because **every word read or written passes through the processor** 
	- Transfer rate is limited by the speed with which the processor can test and service a device
	- The processor is tied up in managing an I/O transfer – a number of instructions must be executed for each I/O transfer

### 3. Direct Memory Access (DMA)
- Used for **high-speed I/O devices** able to transmit information at close to memory speeds
- Performed by a **separate module** on the system bus or incorporated into an I/O module
- Processor **delegates** the I/O operation to DMA by issuing a command containing:
	1. Whether a read or write is requested
	2. The address of the I/O device involved
	3. The starting location in memory to read/write
	4. The number of words to be read/written
- Processor **grants** the I/O module authority to read from or write to memory
- DMA transfers the **entire block of data directly** to and from memory **without processor intervention**
- Only **one interrupt** is generated per block, rather than one interrupt per byte or word
- The processor is involved **only at the beginning and end** of the transfer
	- Processor executes more slowly during a transfer when processor access to the bus is required
- **Advantage**: More efficient than interrupt-driven or programmed I/O for movement of **large volumes of data**

![[FigC-01.png]]
## Computer-System Architecture

### 1. Single-Processor Systems
- Use a single general-purpose processor
- Most systems have special-purpose processors as well (e.g., Disk controller, Keyboard controller, Graphic controller)

### 2. Multiprocessor Systems
- Also known as parallel systems or multicore systems
- Growing in use and importance
- Have two or more processors in close communication
- Processors share the computer bus and sometimes the clock, memory, and peripheral devices

#### Advantages
1. **Performance / Increased throughput**: Greater performance if work can be done in parallel. Speed-up ratio with N processors is < N (due to overhead + contention)
2. **Economy of scale**: Cost less than multiple single-processor systems because they can share peripherals, mass storage, and power supplies
3. **Availability / Increased reliability**: Failure of a single processor does not halt the machine (graceful degradation or fault tolerance)
4. **Incremental Growth**: An additional processor can be added to enhance performance
5. **Scaling**: Vendors can offer a range of products configured with different numbers of processors

#### Types of Multiprocessors
##### 1. Asymmetric Multiprocessing
- Each processor is assigned a specific task (boss–worker relationship)
- The boss processor schedules and allocates work to the workers

##### 2. Symmetric Multiprocessing (SMP)
- A stand-alone computer system with the following characteristics:
	- Two or more similar processors of comparable capability
	- Processors share the same main memory and are interconnected by a bus or other internal connection scheme
	- Processors share access to I/O devices
	- All processors can perform the same functions
- The system is controlled by an integrated operating system that provides interaction between processors and their programs at the job, task, file, and data element levels

![[Fig1-19.png]]


- **Symmetric multiprocessing architecture**
![[Fig1-35.jpg]]

### Multicore Computer
- Also known as a **chip multiprocessor**
- Combines two or more processors (**cores**) on a single piece of silicon (die)
- Each core consists of all the components of an independent processor
- Multicore chips also include L2 cache and in some cases L3 cache
- More efficient than multiple chips with a single core:
	- On-chip communication is faster than between-chip communication
	- One chip with multiple cores uses significantly less power than multiple single-core chips

- **A dual-core design with two cores on the same chip**

![[Fig1-37.jpg]]

![[Fig1-20.png]]
### Clustered Systems
- Multiprocessor systems with multiple systems/nodes joined together
- Each node may be a single processor or multicore
- Closely linked via LAN or InfiniBand
- Usually sharing storage via a Storage-Area Network (SAN)
- Provides a high-availability service which survives failures:
	- A level of redundancy is added: a layer of cluster software runs on the cluster nodes
	- Each node can monitor one or more of the others (over the LAN)
	- If the monitored machine fails, the monitoring machine can take ownership of its storage and restart the applications that were running on the failed machine
- **Asymmetric clustering**: Has one machine in hot-standby mode; others running the applications. The hot-standby host machine does nothing but monitor the active servers. If that server fails, the hot-standby host becomes the active server
- **Symmetric clustering**: Has multiple nodes running applications, monitoring each other. More efficient, as it uses all the available hardware. However, it requires that more than one application be available to run
- **High-Performance Computing (HPC)**: Some clusters run an application concurrently on all computers in the cluster (applications must be written to use parallelization)

- **General structure of a clustered system**

![[Fig1-34.jpg]]

## Appendix

![[FigP-01.png]]

![[FigP-02.png]]

![[FigP-03.png]]

![[FigP-04.png]]

